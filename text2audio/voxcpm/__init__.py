# text2audio — VoxCPM2 backend (local voice cloning)
#
# Self-contained text → audio renderer using local VoxCPM2 voice cloning.
# No dependency on any other backend or shared package.
#
# Usage:
#   python -m voxcpm.render -i story.md -o out.mp3
#   python -m voxcpm.render -i story.md -o out.mp3 --gender Male
